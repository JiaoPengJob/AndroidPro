<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="anim_popup" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/anim_popup_show</item>
        <item name="android:windowExitAnimation">@anim/anim_popup_gone</item>
    </style>

    <style name="AnimTop" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_top_in</item>
        <item name="android:windowExitAnimation">@anim/push_top_out</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        //Dialog的windowFrame框为无
        <item name="android:windowIsFloating">true</item>
        //是否浮现在activity之上
        <item name="android:windowIsTranslucent">false</item>
        //是否半透明
        <item name="android:windowNoTitle">false</item>
        //是否显示title
        <item name="android:windowBackground">@android:color/transparent</item>//设置dialog的背景
        <item name="android:backgroundDimAmount">0.2</item>
        //就是用来控制灰度的值，当为1时，界面除了我们的dialog内容是高亮显示的，dialog以外的区域是黑色的，完全看不到其他内容
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="NavPage">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:orientation">vertical</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:background">@color/navpage</item>
    </style>

    <style name="CalendarWeekStyle">
        <item name="android:background">#000000</item>
        <item name="android:textColor">#444444</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="CalendarTitleStyle">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="CalendarWeekDayStyle">
        <item name="android:colorBackground">#31D09A</item>
        <item name="android:textColor">#999999</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
